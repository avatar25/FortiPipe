apiVersion: v1
kind: Secret
metadata:
  name: {{ include "fortipipe.fullname" . }}-secrets
  labels:
    app: {{ include "fortipipe.name" . }}
stringData:
  secret-key: {{ .Values.secretKey | quote }}
# NOTE: For production, use external secret management or sealed secrets.
