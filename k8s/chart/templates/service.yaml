apiVersion: v1
kind: Service
metadata:
  name: {{ include "fortipipe.fullname" . }}
  labels:
    app: {{ include "fortipipe.name" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "fortipipe.name" . }}
  ports:
    - protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: 8000
